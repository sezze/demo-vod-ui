export default interface ParsedConfiguration {
  baseUrl: string;
  secureBaseUrl: string;
  backdropSizes: number[];
  logoSizes: number[];
  posterSizes: number[];
  profileSizes: number[];
  stillSizes: number[];
}
